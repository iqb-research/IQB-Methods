---
title: "eatPrep"
---

# eatPrep Info

```{r, echo=FALSE}
ansprech_prep <- "Karoline Sachse"
version_prep <- "[aktuelle Version](https://github.com/sachseka/eatPrep/blob/master/DESCRIPTION)"
quelle_prep <- "[github](https://github.com/sachseka/eatPrep), intern"
descFunktion_prep <- "Aufbereitung von Datensätzen (Einlesen, Plausibilitätsprüfungen, Zusammenführen von Datensätzen, Missingbehandlung, Rekodieren, Aggregieren, Scoren und Schreiben von gelabelten SPSS-Datensätzen) unter Verwendung von in der IQB-Datenbank hinterlegten Informationen zu den Items und zum Testdesign. Diese werden mithilfe des ZKDaemon (siehe unten) aus der Datenbank extrahiert und in ein standardisiertes xlsx-Format geschrieben. Dieses xlsx-Format nutzt 'eatPrep', um die Informationen aus der Datenbank nach R zu übertragen."
doku_prep <- "intern"


info_prep <- data.frame(ansprech_prep, version_prep, quelle_prep)
colnames(info_prep) <- c("Ansprechparter:in", "aktuelle Version", "Quelle")

knitr::kable(info_prep)
```

### Funktionsbeschreibung

Aufbereitung von Datensätzen (Einlesen, Plausibilitätsprüfungen, Zusammenführen von Datensätzen, Missingbehandlung, Rekodieren, Aggregieren, Scoren und Schreiben von gelabelten SPSS-Datensätzen) unter Verwendung von in der IQB-Datenbank hinterlegten Informationen zu den Items und zum Testdesign. Diese werden mithilfe des ZKDaemon (siehe unten) aus der Datenbank extrahiert und in ein standardisiertes xlsx-Format geschrieben. Dieses xlsx-Format nutzt 'eatPrep', um die Informationen aus der Datenbank nach R zu übertragen.

**Dokumentation:** (intern)

```{r, include=FALSE}
# Paketname, Ansprechpartner:in, aktuelle Version, Quelle, Funktionsbeschreibung, Dokumentation.



```
